<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Date_Signed_Up__c</fullName>
    <description>Dates pulled from Date FA was signed and Date First Payment</description>
    <formula>IF(
  ISBLANK(Date_First_Payment__c),
  Date_FA_was_signed__c,
  IF(
    ISBLANK(Date_FA_was_signed__c),
    Date_First_Payment__c,
    IF(
      Date_First_Payment__c &lt;= Date_FA_was_signed__c,
      Date_First_Payment__c,
      Date_FA_was_signed__c
    )
  )
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <inlineHelpText>Shows the date the client signed up (the first day they either made a payment or signed a fee agreement)</inlineHelpText>
    <label>Date Signed Up</label>
    <required>false</required>
    <trackTrending>false</trackTrending>
    <type>Date</type>
</CustomField>
